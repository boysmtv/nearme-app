plugins {
    id 'java-library'
}

description = 'DEKAT Subscription - platform subscription plans'

dependencies {
    api project(':sharedkernel')
    api project(':tenant')

    compileOnly 'org.projectlombok:lombok'
    annotationProcessor 'org.projectlombok:lombok'
}
