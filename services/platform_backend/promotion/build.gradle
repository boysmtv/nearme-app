plugins {
    id 'java-library'
}

description = 'DEKAT Promotion - coupons, discounts, referral'

dependencies {
    api project(':sharedkernel')
    api project(':tenant')
    api project(':customer')

    compileOnly 'org.projectlombok:lombok'
    annotationProcessor 'org.projectlombok:lombok'
}
