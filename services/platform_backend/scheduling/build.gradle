plugins {
    id 'java-library'
}

description = 'DEKAT Scheduling - availability, time-slot management'

dependencies {
    api project(':sharedkernel')
    api project(':tenant')
    api project(':staff')
    api project(':catalog')

    compileOnly 'org.projectlombok:lombok'
    annotationProcessor 'org.projectlombok:lombok'
}
